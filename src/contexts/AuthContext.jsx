// Importamos únicamente la función createContext desde React:
import { createContext } from 'react';

export const AuthContext = createContext(); // Creamos el contexto de autenticación con createContext y lo exportamos para su uso en otros componentes.